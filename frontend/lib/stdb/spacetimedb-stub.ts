/**
 * Stub for spacetime:sys@2.0 native module.
 * This module only exists in the SpacetimeDB WASM runtime.
 * The frontend uses the SDK's WebSocket client, not server-side syscalls.
 */
export const moduleHooks = {};
export default {};
